include _mixins.pug
include _variables.pug

section.skills 
  .container
    h2.title.skills__title
      |My tech stack
    .skills__icons-container 
      each skill in skillsList
        +skill-card_m(skill.src, skill.alt)
  